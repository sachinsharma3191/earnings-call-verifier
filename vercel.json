{
  "buildCommand": "npm run build",
  "devCommand": "npm run dev",
  "outputDirectory": "ui/dist",
  "installCommand": "npm install && npm --prefix ui install",
  "rewrites": [
    { "source": "/((?!api).*)", "destination": "/index.html" }
  ]
}
